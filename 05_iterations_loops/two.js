let index = 0;

 while (index <= 10) {
//    console.log(`Value of index is ${index}`);
   index++;
 }

 let heros = ['flash', 'batman', 'madarchod'];

 while (index<heros.length) {
     console.log(heros[index]);
     index++
}
